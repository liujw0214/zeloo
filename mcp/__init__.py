"""MCP (Model Context Protocol) client package.

Provides clients for loading tools from external MCP servers:
* :mod:`mcp.stdio_client` — spawn a local MCP server as a subprocess.
* :mod:`mcp.http_client`  — connect to a remote MCP server over HTTP/SSE.
* :mod:`mcp.tool_filter`  — include/exclude filtering of MCP tools.
"""
