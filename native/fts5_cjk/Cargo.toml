[package]
name = "Zeloo-fts5-cjk"
version = "1.0.0"
edition = "2021"
description = "FTS5 tokenizer for CJK text (Chinese, Japanese, Korean)"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
jieba-rs = "0.6"
libc = "0.2"

[build-dependencies]