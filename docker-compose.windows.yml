#
# docker-compose.windows.yml — Windows Docker Desktop compatible
#
# Differences from docker-compose.yml:
#   - Removes `network_mode: host` (not supported on Docker Desktop for Windows)
#   - Uses explicit port mappings instead
#   - Uses Windows-style volume path for ~/.Zeloo
#
# Usage:
#   docker compose -f docker-compose.windows.yml up -d
#
services:
  gateway:
    image: nousresearch/Zeloo-agent:latest
    container_name: Zeloo
    restart: unless-stopped
    volumes:
      - ${USERPROFILE}/.Zeloo:/opt/data
    environment:
      - ZELOO_UID=10000
      - ZELOO_GID=10000
    command: ["gateway", "run"]

  dashboard:
    image: nousresearch/Zeloo-agent:latest
    container_name: Zeloo-dashboard
    restart: unless-stopped
    depends_on:
      - gateway
    volumes:
      - ${USERPROFILE}/.Zeloo:/opt/data
    environment:
      - ZELOO_UID=10000
      - ZELOO_GID=10000
      - ZELOO_DASHBOARD_HOST=0.0.0.0
    ports:
      - "127.0.0.1:9119:9119"
    command: ["dashboard", "--host", "0.0.0.0", "--port", "9119", "--no-open", "--insecure"]
